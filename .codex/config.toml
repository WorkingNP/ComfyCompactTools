[mcp_servers.comfy_cockpit]
command = "python"
args = ["-m", "server"]
cwd = "C:/Users/souto/Desktop/grok-comfy-cockpit2"
startup_timeout_sec = 30
tool_timeout_sec = 900
enabled = true

[features]
web_search_request = true 

[mcp_servers.comfy_cockpit.env]
COCKPIT_BASE_URL = "http://127.0.0.1:8787"

# Codex MCPツール許可設定
# Codex自身が別のCodexセッションを起動できるようにする
[mcp_clients.codex]
enabled = true
tools = [
  "mcp__codex__codex",
  "mcp__codex__codex-reply"
]
