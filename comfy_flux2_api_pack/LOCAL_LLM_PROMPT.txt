あなたはこのフォルダ一式をレビューして修正するAIです。

必須要件（絶対）
- 実装修正の前に、まずテストを書いてください（既存 tests/ が不十分なら補強）。
- その後、pytest を実行し、失敗する限りは修正→テスト→修正…を繰り返し、最終的に全テストが通るまで止まらないこと。
- テストは ComfyUI ローカルAPI（/prompt→/history→/view）を実際に叩き、このフォルダの workflow（テンプレprompt JSON＋スクリプト）で画像が出力されたことを確認する内容にすること。

制約
- ComfyUIのキャンバス手操作は前提にしない（このフォルダだけで完結）。
- ワークフローはコアノード前提（不用意にカスタムノード依存にしない）。
- 依存ライブラリは最小（requests / Pillow / pytest 程度）。

進め方
1) README.md と FOR_LOCAL_LLM.md を読んで理解する
2) pytest を実行し、失敗内容を整理する
3) 必要なら tests を先に改善して「何を満たすべきか」を明確化
4) 実装を直し、pytest が通るまで反復

成果物
- 修正済みコード
- テスト（通ること）
- READMEの手順が正しく、再現できること
